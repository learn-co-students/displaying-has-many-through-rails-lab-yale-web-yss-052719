<h1><%= @doctor.name %></h1>

<h2><%= @doctor.department %></h2>

<ul>
    <% @doctor.appointments.each do |appt| %>
        <li><%= link_to "#{appt.appointment_datetime.strftime("%B %d, %Y at %R")}, Patient: #{appt.patient.name}", patient_path(appt.patient) %></li>
    <% end %>
</ul>