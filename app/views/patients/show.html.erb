<h1>Patient <%= @patient.name %></h1>
<br>
<h3>Appointments:</h3><br>
<ul>
<% @patient.appointments.each do |app| %>
    <li><%= app.appointment_datetime.strftime("%B %e, %Y at %R") %> -
    <%= link_to app.doctor.name, doctor_path(app.doctor) %></li>
<% end %>
</ul>